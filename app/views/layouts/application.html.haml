%html.no-js
  %head
    = stylesheet_link_tag    "application", :media => "all" 
    = javascript_include_tag "application" 
    = csrf_meta_tags
  
    
    
  %body
     
    #header
      #logo
      #tagline
        %h4
          here, you can only post once a week.  so when you're good and ready, 
    .dropdown
      =link_to "#", :class => "dropdown-toggle", :data => {:toggle=>"dropdown"} do
      
        #post_button
          %h4 sayv something
      %ul.dropdown-menu{:style => "position:relative; top: 60px; width: 100px; right: 10px;"}
        %li text
        %li image
        %li video 
    #left_sidebar
      #home_button
      #popular_button
      #scrapbooks_button
      #archives_button
      #profile_button
      = link_to 'logout',  destroy_user_session_path
    #main_content
      = yield
  
